import 'package:flutter/material.dart';

class FriendTitle extends StatelessWidget {
  const FriendTitle({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(children: [],),
    );
  }
}
